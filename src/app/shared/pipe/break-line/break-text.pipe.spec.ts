import { BreakTextPipe } from './break-text.pipe';

describe('BreakTextPipe', () => {
  it('create an instance', () => {
    const pipe = new BreakTextPipe();
    expect(pipe).toBeTruthy();
  });
});
